# 📊 Application Architecture & Flow

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        FRONTEND (Browser)                    │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  index.html  │  │  styles.css  │  │  script.js   │     │
│  │              │  │              │  │              │     │
│  │  - Upload UI │  │  - Gradient  │  │  - Upload    │     │
│  │  - Form UI   │  │  - Cards     │  │  - Send      │     │
│  │  - Messages  │  │  - Buttons   │  │  - Validate  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
└─────────────────────────┬────────────────────────────────────┘
                          │ HTTP REST API
                          │ (Port 8080)
┌─────────────────────────▼────────────────────────────────────┐
│                   BACKEND (Spring Boot)                       │
│                                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           EmailController (@RestController)           │   │
│  │  /email/upload  /email/send  /email/count  /clear    │   │
│  └─────────┬────────────┬──────────────────────────────┘   │
│            │            │                                    │
│  ┌─────────▼────┐  ┌───▼─────────────────────────────────┐ │
│  │ ExcelReader  │  │      EmailService (@Async)         │ │
│  │   Service    │  │                                     │ │
│  │              │  │  - sendBulkEmails()                │ │
│  │  - Read CSV  │  │  - Background processing           │ │
│  │  - Read XLSX │  │  - Individual email sending        │ │
│  │  - Validate  │  │  - Success/Error logging           │ │
│  └──────────────┘  └────────────┬───────────────────────┘ │
│                                  │                          │
│                         ┌────────▼────────┐                 │
│                         │ JavaMailSender  │                 │
│                         │   (Spring Mail) │                 │
│                         └────────┬─────────┘                │
└──────────────────────────────────┼──────────────────────────┘
                                   │ SMTP
                                   │ (Port 587)
                          ┌────────▼────────┐
                          │  Gmail SMTP     │
                          │  smtp.gmail.com │
                          └────────┬─────────┘
                                   │
                          ┌────────▼────────┐
                          │   Recipients    │
                          │   📧 📧 📧      │
                          └─────────────────┘
```

---

## 🔄 Email Sending Flow

```
┌────────────┐
│   USER     │
└─────┬──────┘
      │ 1. Select file
      ▼
┌────────────────────┐
│  Frontend (UI)     │
│  - Validate file   │
│  - Show preview    │
└─────┬──────────────┘
      │ 2. HTTP POST /email/upload (FormData)
      ▼
┌────────────────────────────────┐
│  EmailController               │
│  @PostMapping("/upload")       │
└─────┬──────────────────────────┘
      │ 3. Parse file
      ▼
┌────────────────────────────────┐
│  ExcelReaderService            │
│  - Read Excel/CSV              │
│  - Extract emails (Column 1)   │
│  - Validate format             │
│  - Return List<String>         │
└─────┬──────────────────────────┘
      │ 4. Store in memory
      ▼
┌────────────────────────────────┐
│  Controller: uploadedEmails    │
│  List<String>                  │
└─────┬──────────────────────────┘
      │ 5. Return success response
      ▼
┌────────────────────┐
│  Frontend (UI)     │
│  - Show count      │
│  - Enable send     │
└─────┬──────────────┘
      │ 6. Enter subject & message
      │ 7. Click "Send"
      ▼
┌────────────────────┐
│  Frontend (UI)     │
│  - Validate form   │
│  - Confirm action  │
└─────┬──────────────┘
      │ 8. HTTP POST /email/send (JSON)
      ▼
┌────────────────────────────────┐
│  EmailController               │
│  @PostMapping("/send")         │
│  - Validate request            │
│  - Check emails exist          │
└─────┬──────────────────────────┘
      │ 9. Call async service
      ▼
┌────────────────────────────────┐
│  EmailService                  │
│  @Async sendBulkEmails()       │
└─────┬──────────────────────────┘
      │ 10. Return immediately
      ▼
┌────────────────────┐
│  Frontend (UI)     │
│  - Show success    │
│  - "Sending..."    │
└────────────────────┘

      ┌─────────────────────────┐
      │  Meanwhile...           │
      │  (Background Thread)    │
      └─────┬───────────────────┘
            │ For each email:
            ▼
      ┌─────────────────────────┐
      │  EmailService           │
      │  - Create message       │
      │  - Set to/subject/text  │
      └─────┬───────────────────┘
            │ Send via SMTP
            ▼
      ┌─────────────────────────┐
      │  JavaMailSender         │
      │  - Connect to Gmail     │
      │  - Authenticate         │
      │  - Send email           │
      └─────┬───────────────────┘
            │ SMTP Protocol
            ▼
      ┌─────────────────────────┐
      │  Gmail SMTP             │
      │  smtp.gmail.com:587     │
      │  - TLS Encryption       │
      └─────┬───────────────────┘
            │ Deliver
            ▼
      ┌─────────────────────────┐
      │  Recipient Inbox 📧     │
      └─────────────────────────┘

      ┌─────────────────────────┐
      │  Logs (for each email)  │
      │  ✓ Success: email@...   │
      │  ✗ Failed: email@...    │
      └─────────────────────────┘
```

---

## 📦 Data Flow Diagram

```
┌──────────────┐
│  Excel/CSV   │
│  File        │
└──────┬───────┘
       │
       │ email1@example.com
       │ email2@example.com
       │ email3@example.com
       ▼
┌──────────────────┐
│  Upload Endpoint │
│  /email/upload   │
└──────┬───────────┘
       │
       │ MultipartFile
       ▼
┌──────────────────┐
│ ExcelReader      │
│ Service          │
└──────┬───────────┘
       │
       │ List<String> emails
       ▼
┌──────────────────┐
│ In-Memory Store  │
│ (Controller)     │
└──────┬───────────┘
       │
       │ Retrieve
       ▼
┌──────────────────┐
│ Email Service    │
│ + Subject        │
│ + Message        │
└──────┬───────────┘
       │
       │ For each email
       ▼
┌──────────────────┐
│ SimpleMailMessage│
│ - to: email      │
│ - subject: X     │
│ - text: Y        │
└──────┬───────────┘
       │
       │ Send via SMTP
       ▼
┌──────────────────┐
│ Gmail Server     │
└──────┬───────────┘
       │
       │ Deliver
       ▼
┌──────────────────┐
│ Recipients       │
└──────────────────┘
```

---

## 🎯 Request/Response Flow

### Upload Request
```
Browser                Controller              ExcelReader
   │                       │                       │
   │  POST /email/upload   │                       │
   ├──────────────────────>│                       │
   │  FormData(file)       │  readEmailsFromFile() │
   │                       ├──────────────────────>│
   │                       │                       │
   │                       │   List<String> emails │
   │                       │<──────────────────────┤
   │                       │                       │
   │  ApiResponse          │                       │
   │<──────────────────────┤                       │
   │  {success, data:10}   │                       │
   │                       │                       │
```

### Send Request
```
Browser                Controller              EmailService
   │                       │                       │
   │  POST /email/send     │                       │
   ├──────────────────────>│                       │
   │  {subject, message}   │  sendBulkEmails()     │
   │                       ├──────────────────────>│
   │                       │  @Async (background)  │
   │                       │                       │
   │  ApiResponse          │                       │
   │<──────────────────────┤                       │
   │  "Sending started"    │                       │
   │                       │                       │
   │                       │    [Background Thread]│
   │                       │    For each email:    │
   │                       │    - Send             │
   │                       │    - Log result       │
   │                       │                       │
```

---

## 🏢 Component Responsibilities

### Frontend Layer
```
┌─────────────────────────────────────────┐
│  index.html                             │
│  - Structure                            │
│  - Upload section                       │
│  - Email form                           │
│  - Status messages                      │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  styles.css                             │
│  - Visual design                        │
│  - Gradient background                  │
│  - Button styling                       │
│  - Responsive layout                    │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  script.js                              │
│  - uploadFile()                         │
│  - sendEmails()                         │
│  - Form validation                      │
│  - API communication                    │
│  - Error handling                       │
└─────────────────────────────────────────┘
```

### Backend Layer
```
┌─────────────────────────────────────────┐
│  EmailController.java                   │
│  - HTTP endpoints                       │
│  - Request validation                   │
│  - Response formatting                  │
│  - Service orchestration                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  EmailService.java                      │
│  - @Async email sending                 │
│  - Bulk processing                      │
│  - Error handling                       │
│  - Logging                              │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  ExcelReaderService.java                │
│  - Excel parsing (Apache POI)           │
│  - CSV parsing                          │
│  - Email validation                     │
│  - Format detection                     │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  EmailRequest.java                      │
│  - Subject field                        │
│  - Message field                        │
│  - Lombok annotations                   │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│  ApiResponse.java                       │
│  - Success flag                         │
│  - Message text                         │
│  - Optional data                        │
└─────────────────────────────────────────┘
```

---

## ⚙️ Configuration Flow

```
┌─────────────────────────┐
│ application.properties  │
└──────────┬──────────────┘
           │
           ├─> Spring Boot Auto-config
           │   - JavaMailSender bean
           │   - SMTP settings
           │
           ├─> @EnableAsync
           │   - Thread pool
           │   - Async executor
           │
           └─> MultipartConfig
               - Max file size
               - Max request size
```

---

## 🔐 Security Flow

```
Gmail Account
    │
    ├─> 2FA Enabled
    │
    └─> App Password Generated
            │
            └─> application.properties
                    │
                    └─> JavaMailSender
                            │
                            └─> SMTP Connection
                                    │
                                    └─> TLS/STARTTLS
                                            │
                                            └─> Authenticated
```

---

## 📊 State Management

```
Application Start
    │
    └─> Empty List: uploadedEmails = []
            │
            ├─> Upload file
            │   └─> uploadedEmails = [email1, email2, ...]
            │
            ├─> Send emails
            │   └─> Read from uploadedEmails
            │       Create copies for async processing
            │
            └─> Clear
                └─> uploadedEmails = []
```

---

## 🔄 Async Processing

```
Main Thread (Request Handler)
    │
    ├─> Receive request
    ├─> Validate input
    ├─> Call @Async method
    └─> Return response immediately ✓

Background Thread (Async Executor)
    │
    ├─> Receive email list
    ├─> For each email:
    │   ├─> Create message
    │   ├─> Send via SMTP
    │   ├─> Log result
    │   └─> Continue
    └─> Complete (logged)
```

---

## 📈 Success Path

```
1. User uploads emails.xlsx
   ✓ File validated
   ✓ 10 emails extracted
   ✓ All emails valid format

2. User enters subject and message
   ✓ Required fields filled
   ✓ No validation errors

3. User clicks "Send Bulk Emails"
   ✓ Confirmation accepted

4. Backend processes request
   ✓ Email list exists
   ✓ Request validated
   ✓ Async service called

5. Background sending
   ✓ Email 1: Sent
   ✓ Email 2: Sent
   ✓ ...
   ✓ Email 10: Sent

6. Completion
   ✓ Success: 10, Failed: 0
   ✓ All logs recorded
```

---

## ⚠️ Error Handling Path

```
Upload Errors:
├─> No file selected
│   └─> "Please select a file"
├─> Invalid format (.txt)
│   └─> "Unsupported file format"
├─> No valid emails
│   └─> "No valid email addresses found"
└─> File too large
    └─> "File size exceeds limit"

Send Errors:
├─> No emails uploaded
│   └─> "Please upload a file first"
├─> Missing subject
│   └─> "Email subject is required"
├─> Missing message
│   └─> "Email message is required"
└─> SMTP error
    └─> Logged, continue with next email
```

---

This diagram shows the complete flow and architecture of your bulk email sender application!

